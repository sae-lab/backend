name: Backend CI and container package

on:
  pull_request:
    branches:
      - dev
      - main
  push:
    branches:
      - dev
      - main
  workflow_dispatch:
    inputs:
      candidate_sha:
        description: "Full commit SHA from the dev branch to publish as a GHCR candidate"
        required: true
        type: string

concurrency:
  group: backend-container-${{ github.workflow }}-${{ github.event.inputs.candidate_sha || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    outputs:
      image_revision: ${{ steps.image_revision.outputs.sha }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: sightseeing
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Validate candidate SHA format
        if: github.event_name == 'workflow_dispatch'
        env:
          CANDIDATE_SHA: ${{ inputs.candidate_sha }}
        run: |
          if [[ ! "${CANDIDATE_SHA}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "candidate_sha must be a lowercase, full 40-character commit SHA" >&2
            exit 1
          fi

      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.candidate_sha || github.sha }}
          fetch-depth: ${{ github.event_name == 'workflow_dispatch' && '0' || '1' }}

      - name: Verify manual candidate is a dev commit
        if: github.event_name == 'workflow_dispatch'
        env:
          CANDIDATE_SHA: ${{ inputs.candidate_sha }}
        run: |
          set -euo pipefail

          git rev-parse --verify "${CANDIDATE_SHA}^{commit}" >/dev/null
          actual_sha="$(git rev-parse --verify HEAD)"
          if [[ "${actual_sha}" != "${CANDIDATE_SHA}" ]]; then
            echo "Checked-out commit does not match candidate_sha" >&2
            exit 1
          fi

          git fetch --no-tags origin +refs/heads/dev:refs/remotes/origin/dev
          if ! git merge-base --is-ancestor "${CANDIDATE_SHA}" refs/remotes/origin/dev; then
            echo "candidate_sha is not contained in the dev branch history" >&2
            exit 1
          fi

      - name: Resolve image revision
        id: image_revision
        run: echo "sha=$(git rev-parse --verify HEAD)" >> "${GITHUB_OUTPUT}"

      - name: Set up Java 17
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Test and package application
        run: ./gradlew --no-daemon clean test bootJar

      - name: Build container image
        run: docker build --tag sightseeing-backend-prod:test .

      - name: Start application container
        run: |
          set -euo pipefail

          JWT_SECRET="$(openssl rand -hex 64)"
          export JWT_SECRET
          echo "::add-mask::${JWT_SECRET}"

          docker run --detach \
            --name sightseeing-backend-smoke \
            --add-host host.docker.internal:host-gateway \
            --publish 8080:8080 \
            --env PORT=8080 \
            --env DB_URL=jdbc:postgresql://host.docker.internal:5432/sightseeing \
            --env DB_USERNAME=postgres \
            --env DB_PASSWORD=postgres \
            --env JWT_SECRET \
            --env API_TOKEN=ci-placeholder \
            --env TOUR_API_BASE_URL=https://apis.data.go.kr/B551011/KorService2 \
            --env ROUTE_API_BASE_URL=https://apis.data.go.kr/B551011/Durunubi \
            --env KAKAO_API_KEY=ci-placeholder \
            --env STORAGE_TYPE=local \
            --env APP_SEED_ENABLED=false \
            sightseeing-backend-prod:test

      - name: Wait for application readiness
        run: |
          set -euo pipefail

          for attempt in $(seq 1 60); do
            if curl --silent --fail --max-time 2 http://127.0.0.1:8080/readyz >/dev/null; then
              echo "Application readiness probe is UP"
              exit 0
            fi

            if ! docker inspect --format='{{.State.Running}}' sightseeing-backend-smoke 2>/dev/null | grep -q '^true$'; then
              echo "Application container exited before becoming ready" >&2
              docker logs sightseeing-backend-smoke >&2 || true
              exit 1
            fi

            sleep 1
          done

          echo "Application did not become ready within 60 seconds" >&2
          docker logs sightseeing-backend-smoke >&2 || true
          exit 1

      - name: Verify health endpoints
        run: bash scripts/verify-health-endpoints.sh http://127.0.0.1:8080 up

      - name: Show application logs on failure
        if: failure()
        run: docker logs sightseeing-backend-smoke || true

      - name: Stop application container
        if: always()
        run: docker rm --force sightseeing-backend-smoke || true

  publish:
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && needs.validate.outputs.image_revision != '')
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    concurrency:
      group: backend-container-publish-${{ needs.validate.outputs.image_revision }}
      cancel-in-progress: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ needs.validate.outputs.image_revision }}
          persist-credentials: false

      - name: Verify checkout credentials are not persisted
        run: |
          if git config --local --get-regexp '^http\..*\.extraheader$' >/dev/null; then
            echo "Checkout credentials must not remain in the candidate build context" >&2
            exit 1
          fi

      - name: Log in to inspect GitHub Container Registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1 # v3.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Inspect immutable image tag
        id: immutable_image
        env:
          IMAGE: ghcr.io/sae-lab/backend
          IMAGE_REVISION: ${{ needs.validate.outputs.image_revision }}
        run: |
          set -euo pipefail

          if inspect_output="$(docker manifest inspect "${IMAGE}:${IMAGE_REVISION}" 2>&1)"; then
            echo "exists=true" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          if [[ "${inspect_output}" != *"manifest unknown"* && "${inspect_output}" != *"no such manifest"* ]]; then
            echo "Unable to safely determine whether the candidate image tag exists" >&2
            echo "${inspect_output}" >&2
            exit 1
          fi

          echo "exists=false" >> "${GITHUB_OUTPUT}"

      - name: Reject an existing manual candidate image
        if: github.event_name == 'workflow_dispatch' && steps.immutable_image.outputs.exists == 'true'
        run: |
          echo "Candidate image tag already exists and will not be overwritten" >&2
          exit 1

      - name: Promote an existing immutable image to main
        if: >-
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          steps.immutable_image.outputs.exists == 'true'
        env:
          IMAGE: ghcr.io/sae-lab/backend
          IMAGE_REVISION: ${{ needs.validate.outputs.image_revision }}
        run: |
          set -euo pipefail

          docker pull "${IMAGE}:${IMAGE_REVISION}"
          existing_revision="$(docker image inspect \
            --format '{{ index .Config.Labels "org.opencontainers.image.revision" }}' \
            "${IMAGE}:${IMAGE_REVISION}")"
          existing_source="$(docker image inspect \
            --format '{{ index .Config.Labels "org.opencontainers.image.source" }}' \
            "${IMAGE}:${IMAGE_REVISION}")"

          if [[ "${existing_revision}" != "${IMAGE_REVISION}" ]]; then
            echo "Existing immutable image has an unexpected revision label" >&2
            exit 1
          fi
          if [[ "${existing_source}" != "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" ]]; then
            echo "Existing immutable image has an unexpected source label" >&2
            exit 1
          fi

          immutable_ref="$(docker image inspect \
            --format '{{ index .RepoDigests 0 }}' \
            "${IMAGE}:${IMAGE_REVISION}")"
          if [[ "${immutable_ref}" != "${IMAGE}@sha256:"* ]]; then
            echo "Unable to resolve the existing immutable image digest" >&2
            exit 1
          fi
          expected_digest="${immutable_ref##*@}"

          source_manifest="$(docker buildx imagetools inspect --raw "${immutable_ref}")"
          docker buildx imagetools create \
            --prefer-index=false \
            --tag "${IMAGE}:main" \
            "${immutable_ref}"
          main_digest="$(docker buildx imagetools inspect \
            --format '{{json .Manifest}}' \
            "${IMAGE}:main" | jq -r '.digest')"
          if [[ "${main_digest}" != "${expected_digest}" ]]; then
            echo "The promoted main tag does not resolve to the immutable image digest" >&2
            exit 1
          fi
          main_manifest="$(docker buildx imagetools inspect --raw "${IMAGE}:main")"
          if [[ "${main_manifest}" != "${source_manifest}" ]]; then
            echo "The promoted main tag does not match the immutable image manifest" >&2
            exit 1
          fi

      - name: Remove registry credentials before building candidate content
        if: steps.immutable_image.outputs.exists == 'false'
        run: docker logout ghcr.io

      - name: Build new immutable container image
        if: steps.immutable_image.outputs.exists == 'false'
        env:
          IMAGE: ghcr.io/sae-lab/backend
          IMAGE_REVISION: ${{ needs.validate.outputs.image_revision }}
        run: |
          set -euo pipefail

          build_args=(
            --label "org.opencontainers.image.revision=${IMAGE_REVISION}"
            --label "org.opencontainers.image.source=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
            --tag "${IMAGE}:${IMAGE_REVISION}"
          )
          if [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/heads/main" ]]; then
            build_args+=(--tag "${IMAGE}:main")
          fi

          docker build "${build_args[@]}" .

      - name: Log in to publish new immutable image
        if: steps.immutable_image.outputs.exists == 'false'
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1 # v3.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Recheck immutable image tag before publishing
        if: steps.immutable_image.outputs.exists == 'false'
        env:
          IMAGE: ghcr.io/sae-lab/backend
          IMAGE_REVISION: ${{ needs.validate.outputs.image_revision }}
        run: |
          set -euo pipefail

          if inspect_output="$(docker manifest inspect "${IMAGE}:${IMAGE_REVISION}" 2>&1)"; then
            echo "Immutable image tag appeared during the build and will not be overwritten" >&2
            exit 1
          fi
          if [[ "${inspect_output}" != *"manifest unknown"* && "${inspect_output}" != *"no such manifest"* ]]; then
            echo "Unable to safely recheck whether the immutable image tag exists" >&2
            echo "${inspect_output}" >&2
            exit 1
          fi

      - name: Publish new immutable container image
        if: steps.immutable_image.outputs.exists == 'false'
        env:
          IMAGE: ghcr.io/sae-lab/backend
          IMAGE_REVISION: ${{ needs.validate.outputs.image_revision }}
        run: |
          set -euo pipefail

          docker push "${IMAGE}:${IMAGE_REVISION}"

          if [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/heads/main" ]]; then
            docker push "${IMAGE}:main"
          fi
